import type { Block } from 'payload'
export const Badge: Block = {
  slug: 'Badge',
  interfaceName: 'Badge',
  fields: [
    {
      name: 'content',
      label: 'Content',
      type: 'text'
    },
  ],
}
